import styled from "@emotion/styled";
import { theme } from "@/shared/theme/theme";

export const ScreenlogLayout = styled.div`
  background-color: ${theme.colors.background.page};
  padding: ${theme.spacing.none} 10%;
  min-height: 100dvh;
`;
